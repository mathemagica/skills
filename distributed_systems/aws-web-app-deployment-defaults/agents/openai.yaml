interface:
  display_name: "AWS Web App Deployment Defaults"
  short_description: "Plan secure AWS web app deployments"
  default_prompt: "Use $aws-web-app-deployment-defaults to design a secure, scalable, observable AWS deployment for this web app."

policy:
  allow_implicit_invocation: true
